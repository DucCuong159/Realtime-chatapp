{
  "name": "realtime-chatapp",
  "private": true,
  "description": "Realtime chat application - monorepo root",
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "install:all": "yarn install && yarn --cwd frontend install && yarn --cwd backend install"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2"
  }
}
